discord.py[voice]==2.0.0  # Regular Discord functionality + Voice support
python-dotenv==0.19.2     # To load environment variables like your token
aiohttp==3.9.5            # Required by discord.py for HTTP communication
multidict==6.0.5          # Required by aiohttp for multi-dictionary handling
attrs==23.2.0             # Utility for managing classes and data structures
yarl==1.9.4               # URL manipulation used by aiohttp
frozenlist==1.4.1         # Provides list-like containers used in async environments
async-timeout==4.0.3      # Handles asynchronous timeouts
idna==3.7                 # Required by aiohttp for international domain names
charset-normalizer==3.3.2 # For encoding and decoding text (required by aiohttp)
aiosignal==1.3.1         # Signal handling for async apps like discord.py
PyNaCl==1.5.0            # Required for voice support in discord.py (audio functionality)